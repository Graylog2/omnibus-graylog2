# This file is managed by graylog2-ctl. Manual changes will be
# erased! To change the contents below, edit /etc/graylog2/graylog2.rb
# and run `sudo graylog2-ctl reconfigure`.

[user]
        name = <%= node['graylog2']['user']['git_user_name'] %>
        email = <%= node['graylog2']['user']['git_user_email'] %>
[core]
        autocrlf = input
